import { FirebaseQuery } from "../types";
export declare type FirebaseJoin = {
    joinWhen: ({ path, value }: {
        path: string;
        value: any;
    }) => boolean;
};
export declare type FirebaseDataViewProps = {
    root: FirebaseQuery;
    joins: FirebaseQuery[];
};
